<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/style.css">

    <title>Document</title>
</head>

<body>
<div class="container">
    <h1 class="text-center">{{shop.title}}</h1>
    <div class="col-md-6 col-md-offset-3">
        <form action="/hotelluxuz/shop/update/{{shop._id}}" method="Post">

            <div class="input">
                <label for="title">Title:</label>
                <input class="form-control" type="text" name="title" value="{{shop.title}}">
            </div>
            <br>
            <div class="input">
                <label for="image">Image Url:</label>
                <input class="form-control" type="text" name="image" value="{{shop.image}}">
            </div>
            <br>

            <div class="input">
                <label for="price">Price $:</label>
                <input class="form-control" type="text" name="price" value="{{shop.price}}">
            </div>

            <br>
            <div class="input">
                <label for="description">Description:</label>
                <textarea class="form-control"  name="description" >{{shop.description}}</textarea>
            </div>
            <br>

           <div class="buttons">
            <button type="submit"  class="btn btn-success">Update Product</button>

            <a type="submit" href="/hotelluxuz/shop/{{shop._id}}" class="btn btn-primary">BacK to Product</a>
            <a type="submit" href="/hotelluxuz/shop" class="btn btn-info">BacK to Products Page</a>
            </div>




        </form>

    </div>
</div>
</body>
</html>